bluetron-nb-common:
  apilog:
    enable: true
    showip: true
    showargs: false
#    auditlog:
#      type: db
#      datasource:
spring:
  application:
    name: common-sb
  datasource:
#    url: jdbc:mysql://${MYSQL_IP:192.168.8.133}:${MYSQL_PORT:53306}/bluetron-common?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    url: jdbc:mysql://${MYSQL_IP:localhost}:${MYSQL_PORT:3306}/world?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000